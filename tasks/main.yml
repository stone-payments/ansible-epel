---
- name: install EPEL repository
  yum_repository:
    name: epel
    state: present
    description: EPEL YUM repo
    baseurl: "{{ epel_repo_url }}"
    gpgkey: "{{ epel_gpg_url }}"

- name: update cache
  command: yum clean metadata
  register: yum_result
  changed_when: yum_result.rc != 0